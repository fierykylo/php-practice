<?php
    $username = "Bro Code";
    //$username = strtolower()
    // strtoupper
    //trim => removes whitespaces
    //str_replace("-","",$phone)
    //strrev => reverse
    //str_shuffle = 
    //strcmp ($var, "string") if both are same this returns 0
    //strlen -> returns the length of the string
    //strpos($number, "1")
    // $firstname = substr($username, 0, 3);
    //explode turns string into an array
    // implode turns an array into a string 
    
?>